[build]
builder = "NIXPACKS"
buildCommand = "rm -rf .next node_modules/.cache && npm install && npm run build"

[deploy]
startCommand = "next start -p ${PORT:-3001}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
